issuer: https://accounts.google.com
# The unique ID of the service account that ghreconciler runs as
subject: "113456058529792035155"

permissions:
  # Required to push commits with fixes to the fork
  contents: write
  # Required to fix GitHub Actions workflow files
  workflows: write

# You can limit the scope of what autofix can access via this list
repositories:
  - terraform-cloudrun-glue
  - enterprise-packages
  - mono
